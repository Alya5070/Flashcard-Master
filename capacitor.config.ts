import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flashmaster.app',
  appName: 'FlashMaster',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
